# CloudTrail Lab — Final Conclusion (S3 Data Events)

## 🧠 Observation
During testing, S3 object-level actions (GetObject / DeleteObject) did not consistently appear in CloudTrail Event History or expected log views.

Both:
- CLI actions
- Console actions

did not show up as expected in Event History.
